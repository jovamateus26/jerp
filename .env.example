# Registry configuration used by the compose stack and CI pipeline
REGISTRY_URL=registry.example.com
REGISTRY_IMAGE_NAME=erpnext-custom
REGISTRY_IMAGE_TAG=15-prod

# Database credentials
MARIADB_ROOT_PASSWORD=change-me

# Default ERPNext Administrator password used by automation scripts
FRAPPE_ADMIN_PASSWORD=change-me-too

# Optional: Portainer webhook URL to trigger stack updates after publishing new images
PORTAINER_WEBHOOK_URL=https://portainer.example.com/api/webhooks/uuid
