#!/usr/bin/env bash
set -euo pipefail

# TODO: Backup every site, run bench migrate, clear cache, and rebuild assets for all tenants.

echo "migrate_all.sh is a placeholder. Implement the migration routine."
